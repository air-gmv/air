<!-- Test description file -->
<!-- Test steps must be sequential, starting at 001 -->
<!-- Repetitions are limited to 0xFFF,i.e. 4095		-->
<!-- Major time frame [seconds]						-->
<!-- Test End partition : partition where the test
	 will end and finish - if needed, this can be 
	 adapted										-->
<TDF>
	<!-- Test Definition -->
	<test 	name="TEST-DEF-80030"
			id="80030" 
			partitions="1"
			test_end_partition="0"
			repeat="1" 
			mtf="0.3"
			ms_per_tick="10"
			type="TEST_GMV_VAL"
			personality="RTEMS5">
		<!-- trap number should be in hexadecimal format -->
		<registered_traps async="" sync="0x09" />			
		<description>
			Define a constant string (which the compiler shall locate in the
			Text area) in the code. Get its address, and try to rewrite it using
			pointer indirection (to avoid compiler detection). Each attempt shall raise
			a Health Monitor event that shall be captured.
		</description>
		<requirements>
			<req name="AIR_SEP_0020" />	
		</requirements>
		
		<!-- Test Procedure -->
		<testprocedure repeat="1">
			<teststep id="0" partition="0" repeat="1">
				<description>
					Define a char pointer to a constant string.
				</description>
			</teststep>
			<teststep id="1" partition="0" repeat="1">
				<description>
					Try to write to the obtained location.
				</description>
			</teststep>
			<teststep id="2" partition="0" repeat="1">
				<description>
					Check that the hm_part_callback was called with the i_error parameter set to
					PMK_SEG_ERR.
				</description>
			</teststep>			
			
		</testprocedure>		
	</test>	<!-- Test id="050" -->
</TDF>
