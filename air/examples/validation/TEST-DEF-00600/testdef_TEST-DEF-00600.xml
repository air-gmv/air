<!-- Test description file -->
<!-- Test steps must be sequential, starting at 001 -->
<!-- Repetitions are limited to 0xFFF,i.e. 4095		-->
<!-- Major time frame [seconds]						-->
<!-- Test End partition : partition where the test
	 will end and finish - if needed, this can be 
	 adapted										-->
<TDF>
	<!-- Test Definition -->
	<test 	name="TEST-DEF-00600"
			id="600" 
			partitions="2"
			test_end_partition="0"
			repeat="1" 
			mtf="0.3"
			ms_per_tick="10"
			type="TEST_GMV_VAL"
			personality="RTEMSIMPR">
		<!-- trap number should be in hexadecimal format -->
		<registered_traps async="" sync="0x03" />			
		<description>
						Define a test with two partitions, an application partition with
			non-supervisor permissions, and a system partition, with supervisor permission.
			Test if the execution of a supervisor mode instruction (read psr
			contents) in the application partition is captured and handled by the HM, and
			the associated HM action is called. Test if the remaining partitions
			continue to execute. Repeat the test in the system partition and check that
			no HM event is raised.
		</description>
		<requirements>
			<req name="AIR_SEP_0300" />	
			<req name="AIR_SEP_0310" />	
		</requirements>
		
		<!-- Test Procedure -->
		<testprocedure repeat="1">
			<teststep id="0" partition="0" repeat="1">
				<description>
					Execute a supervisor instruction [e.g. lda instruction]. Verify that the HM is not raised.
				</description>
			</teststep>
			<teststep id="1" partition="0" repeat="1">
				<description>
					Wait for next execution window
				</description>
			</teststep>
			<teststep id="2" partition="1" repeat="1">
				<description>
					Execute a regular operation.
				</description>
			</teststep>
			<teststep id="3" partition="1" repeat="1">
				<description>
					Execute a supervisor instruction [e.g. lda instruction]. Verify that the trap is raised.
				</description>
			</teststep>
			<teststep id="4" partition="1" repeat="1">
				<description>
					idle the partition
				</description>
			</teststep>
			<teststep id="5" partition="0" repeat="1">
				<description>
					Execute a regular operation.
				</description>
			</teststep>			
			
		</testprocedure>		
	</test>	<!-- Test id="050" -->
</TDF>