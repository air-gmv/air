<!-- Test description file -->
<!-- Test steps must be sequential, starting at 001 -->
<!-- Repetitions are limited to 0xFFF,i.e. 4095		-->
<!-- Major time frame [seconds]						-->
<!-- Test End partition : partition where the test
	 will end and finish - if needed, this can be 
	 adapted										-->
<TDF>
	<!-- Test Definition -->
	<test 	name="TEST-DEF-90004"
			id="90004" 
			partitions="2"
			test_end_partition="0"
			repeat="1" 
			mtf="1.5"
			ms_per_tick="10"
			type="TEST_GMV_VAL"
			personality="RTEMSIMPR">
		<description>
			Configure a test with 2 partitions, in which one of them call a
			function that sets the stack pointer to 0. Checks if that function
			blocks or not the other partition.
		</description>
		<requirements>
			<req name="AIR_SEP_0120" />	
		</requirements>
		
		<!-- Test Procedure -->
		<testprocedure repeat="1">
      <teststep id="0" partition="0" repeat="1">
        <description>
            Call the function pmk_null_stack_pointer.
        </description>
      </teststep>
      <teststep id="1" partition="0" repeat="1">
        <description>
            Set stack pointer to 0.
        </description>
      </teststep>
      <teststep id="2" partition="0" repeat="1">
        <description>
            Sleep for 50 clock ticks, so the output is shown in order.
        </description>
      </teststep>
      <teststep id="3" partition="1" repeat="1">
        <description>
            Test if the partition is blocked or not.
        </description>
      </teststep>

		</testprocedure>		
	</test>	<!-- Test id="050" -->
</TDF>
