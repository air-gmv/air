To launch an application on the A53 cpu of the Ultrascale Board run the "launch_a53.sh" script with the path to the executable as an argument. For example:

```
./launch_a53.sh /path/to/executable.elf
```