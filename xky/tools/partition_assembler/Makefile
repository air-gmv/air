#
# Makefile : Partition assembler
# Generated by XKY Configurator Tool v3.5
#

HOST_BUILD=../../install/tools

# Makefile Include file
include ../../Makefile.inc 

EXE=../../install/tools/partition_assembler
SRCS=$(wildcard *.c)
OBJS=$(patsubst %.c,$(HOST_BUILD)/%.o,$(SRCS))
HOST_CPPFLAGS=-Wall -O2 -B./

# All
.PHONY : all
all: $(EXE)
all: $(EXE) $(OBJS)

# Executable
$(EXE): $(OBJS)
	$(HOST_CC) $(OBJS) -o $(EXE)

# Clean
clean:
	@($(RM) ../../install/tools)

# Distclean
.PHONY : distclean
distclean:
	@($(RM) ../../install/tools)
	@($(RM) Makefile)

